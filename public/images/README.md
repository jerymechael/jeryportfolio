# Images

Drop your real portrait/photo and any project cover images here, for example:

- `portrait.jpg` — used in the Hero section (currently a placeholder monogram card)
- `og-image.jpg` — 1200x630 social share image referenced from `app/layout.tsx`

Then update `components/Hero.tsx` to render an `next/image` pointing at
`/images/portrait.jpg` instead of the placeholder monogram block.
