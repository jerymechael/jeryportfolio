import { ContactInfo } from "@/lib/types";
import { whatsappHref } from "@/data/nav";

export const contactInfo: ContactInfo = {
  availability: "Open · 2026",
  basedIn: "West Jakarta, Indonesia",
  response: "Within a day",
  whatsappHref,
};
